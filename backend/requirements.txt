# MigrateMate Backend Dependencies (core)
#
# Everything needed to run the app and the Flask->FastAPI migration. The
# optional local-embedding RAG stack lives in requirements-rag.txt and is only
# needed when ENABLE_VECTOR_SERVICES=true.

# --- Core API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.3
python-multipart==0.0.9   # form/file uploads
python-dotenv==1.0.1
aiofiles==23.2.1
httpx==0.27.0

# --- AI migration (Google Gemini) ---
google-generativeai==0.5.4

# --- Optional infra: Postgres project store + Qdrant/Neo4j/Redis ---
# Imported at startup but only *used* when ENABLE_DATABASE / ENABLE_VECTOR_SERVICES
# are enabled and the matching Docker services are running.
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
greenlet==3.0.3
qdrant-client==1.9.1
neo4j==5.20.0
redis==5.0.4

# --- Testing & dev ---
pytest==8.2.2
pytest-asyncio==0.23.7
black==24.4.2
ruff==0.4.9
